/*
REACT
R-repeat question

we are given n  and we need to find no. of steps require to convert n to 0 by removing digits


for eg, if n=27
27->20->18->10->9->0

no. of steps=5


A-ALGO:

we have base case as if n==0 return 0 
-finding maximum digit
-remove from n
-then call again to function
-return count i.e 1+ the recursive function



C-CODE:

#include <bits/stdc++.h>
using namespace std;

int remove(int n)
{

    if (n == 0)
    {
        return 0;
    }
    int temp = n;
    int digit = INT_MIN;
    while (temp > 0)
    {
        digit = max(digit, temp % 10);
        temp /= 10;
    }
    return 1 + remove(n - digit);
}

int main()
{
    int n, x;
    cin >> n;
    int ans = remove(n);
    cout << ans;
}

T: TEST:
INPUT:
27

OUTPUT: 5
